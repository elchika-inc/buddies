{
  "name": "pawmatch-api-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --local --port 8788",
    "dev:local": "node scripts/run-local.js",
    "build": "wrangler build",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:init": "wrangler d1 execute pawmatch-db --local --file=./schema.sql",
    "db:migrate": "wrangler d1 migrations apply pawmatch-db --local",
    "db:generate": "drizzle-kit generate:sqlite",
    "db:push": "drizzle-kit push:sqlite",
    "db:studio": "drizzle-kit studio",
    "db:migrate:prod": "wrangler d1 migrations apply pawmatch-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "@hono/node-server": "^1.0.0",
    "@types/node": "^20.0.0",
    "sqlite": "^5.0.0",
    "sqlite3": "^5.1.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "drizzle-kit": "^0.31.4",
    "drizzle-orm": "^0.44.4",
    "hono": "^3.12.0"
  }
}
